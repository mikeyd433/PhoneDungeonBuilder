# Supabase project: the-delve
# These are public, client-side keys — the publishable key is designed to be
# shipped in the browser bundle and is safe there. Row Level Security is what
# actually protects the data (see supabase/migrations/0002_rls.sql).
#
# Set these in the Netlify UI for deploys; keep a local copy in .env (gitignored).
VITE_SUPABASE_URL=https://tzfkylvtndgaugbgxbuc.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
